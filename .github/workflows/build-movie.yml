name: Build movie

# Compile l'app Nartya Movies — code source dans le repo PRIVÉ `NartyaTeam/nartya-movie`,
# checkout ici via le PAT dédié (secret `APP_CHECKOUT_TOKEN`, lecture seule, restreint aux
# repos sources déclarés dessus).
#
# Ce repo (`nartya-builds`) est PUBLIC : minutes Actions illimitées. Seul le workflow YAML et
# les binaires compilés sont publics — jamais le code source.
#
# Déclenchement 100% MANUEL, jamais sur `pull_request` (cf. README § sécurité — un fork
# externe pourrait sinon exécuter ce workflow et exfiltrer les secrets).
#
# Ce workflow NE PUBLIE RIEN sur nartya.app — cf. publish-movie.yml, déclenché séparément.
on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Tag ou branche de NartyaTeam/nartya-movie à compiler (ex. v0.2.0)"
        required: true
        type: string

permissions:
  contents: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout du code source privé (NartyaTeam/nartya-movie)
        uses: actions/checkout@v4
        with:
          repository: NartyaTeam/nartya-movie
          ref: ${{ inputs.ref }}
          token: ${{ secrets.APP_CHECKOUT_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build front (Vite)
        run: npm run build
        env:
          # Même projet Supabase que l'app anime (mêmes utilisateurs, SSO via le hub).
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          # URL publique de l'API films (pas un secret — juste l'endpoint prod).
          VITE_MOVIE_API_URL: "https://movie.nartya.app"

      - name: Build Electron main process (electron-dist/)
        run: npm run build:electron

      - name: Build Electron (sans publication)
        run: npx --no-install electron-builder --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"

      - name: Vérifie la présence des métadonnées updater (latest*.yml)
        shell: bash
        run: |
          if ! ls release/latest*.yml >/dev/null 2>&1; then
            echo "❌ Aucun latest*.yml généré — l'updater standalone serait cassé."
            echo "   (Vérifier build.publish=generic dans package.json.)"
            exit 1
          fi
          ls -lh release/

      - name: Vérifie que latest-mac.yml couvre les deux architectures
        if: runner.os == 'macOS'
        shell: bash
        run: |
          for f in release/latest-mac*.yml; do echo "── $f ──"; cat "$f"; done
          agrege=$(cat release/latest-mac*.yml)
          for a in x64 arm64; do
            printf '%s' "$agrege" | grep -q -- "-mac-${a}\." || {
              echo "❌ Aucun artefact ${a} référencé dans les latest-mac*.yml."
              exit 1
            }
          done
          echo "✅ x64 et arm64 sont tous deux référencés."

      - uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.os }}
          path: |
            release/*.exe
            release/*.dmg
            release/*.zip
            release/*.AppImage
            release/*.deb
            release/*.blockmap
            release/latest*.yml
          if-no-files-found: error
          retention-days: 1

  verify:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du code source privé (pour scripts/gen-release-manifest.mjs)
        uses: actions/checkout@v4
        with:
          repository: NartyaTeam/nartya-movie
          ref: ${{ inputs.ref }}
          token: ${{ secrets.APP_CHECKOUT_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Rassemble les artefacts dans upload/
        run: |
          mkdir -p upload
          find artifacts -type f -exec cp -n {} upload/ \;
          ls -lh upload

      - name: Génère manifest.json + SHA256SUMS.txt
        shell: bash
        run: |
          VERSION="${{ inputs.ref }}"
          VERSION="${VERSION#v}"
          node scripts/gen-release-manifest.mjs upload --version "$VERSION"
          ( cd upload && sha256sum "Nartya Movies-"* > SHA256SUMS.txt )
          echo "── manifest.json ──"; cat upload/manifest.json
          echo "── SHA256SUMS.txt ──"; cat upload/SHA256SUMS.txt

      - name: Scan des installeurs sur VirusTotal
        uses: crazy-max/ghaction-virustotal@v4
        with:
          vt_api_key: ${{ secrets.VT_API_KEY }}
          files: |
            upload/Nartya Movies-*.exe
            upload/Nartya Movies-*.dmg
            upload/Nartya Movies-*.AppImage
            upload/Nartya Movies-*.deb

      - uses: actions/upload-artifact@v4
        with:
          name: dist-final
          path: upload/**
          if-no-files-found: error
          retention-days: 5

      - name: Résumé — prochaine étape
        run: |
          echo "✅ Build + vérification terminés pour ${{ inputs.ref }}."
          echo ""
          echo "Run ID de CE run : ${GITHUB_RUN_ID}"
          echo ""
          echo "Pour publier sur le VPS : Actions → 'Publish movie (manual)' → Run workflow"
          echo "→ colle ce run_id : ${GITHUB_RUN_ID}"
